// legacy DOM app neutralized. React components now live in src/
// keep file as a harmless stub to avoid runtime errors if referenced.
console.log('Legacy frontend script neutralized — use React app at /');
